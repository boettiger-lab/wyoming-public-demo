apiVersion: v1
kind: Service
metadata:
  name: wyoming-public-demo
  labels:
    app: wyoming-public-demo
spec:
  type: ClusterIP
  selector:
    app: wyoming-public-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: http
